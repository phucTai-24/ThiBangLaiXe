# Legacy DTOs

This folder contains deprecated DTOs with Vietnamese naming that are no longer used.

## Status: To be removed

These files are not referenced anywhere in the codebase and should be deleted in a future cleanup:

- `auth_dto.cs` - Use `DTOs/Auth/` instead
- `bai_thi_dto.cs` - Use `DTOs/ExamSessions/` instead
- `cau_hoi_dto.cs` - Use `DTOs/Questions/` instead
- `nguoi_dung_dto.cs` - Use `DTOs/Auth/` instead
- And 28 other legacy files...

## Action Required

Delete all .cs files in this folder after verifying no references exist.
